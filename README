# Linux Kernel

## Pre-requisitos

Ter a toolchain `arm-none-eabi` instalada e no seu $PATH

## Helpers

Root helper:

`./helper build` -> compila o kernel do linux
`./helper run` -> inicia o qemu com o kernel linux 
`arch/arm/boot/helper build` -> compila init.c e cria rootfs com init.o gerado
